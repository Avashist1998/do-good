services:
  ui:
    build: 
      context: ./ui
      dockerfile: Dockerfile
    ports:
      - "8090:8080"
    expose:
      - "8080"
    
  pb:
    build:
      context: ./pocketbase
      dockerfile: Dockerfile
    ports:
      - "3010:3000"
    expose:
      - "3000"
    volumes:
      - ./pocketbase/data:/pb/pb_data